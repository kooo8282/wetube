extends layouts/main.pug
include mixins/videoBlock

block content
    .home-videos
        each item in videos
            +videoBlock({
                id:item.id,
                title:item.title,
                views:item.views,
                videoUrl:item.fileUrl
            })

            +videoBlock({
                id:item.id,
                title:item.title,
                views:item.views,
                videoUrl:item.fileUrl
            })

            +videoBlock({
                id:item.id,
                title:item.title,
                views:item.views,
                videoUrl:item.fileUrl
            })